# Domain map

- `akimitsu.store` — owned measurement landing.
- `menu.akimitsu.store` — same landing surface, intended for campaign/menu links.
- `go.akimitsu.store` — tracked redirect gateway; `/menu`, `/order`, `/maps`, `/call` map to current restaurant destinations.

All hosts may terminate at one runtime service. Hostinger remains DNS authority.
