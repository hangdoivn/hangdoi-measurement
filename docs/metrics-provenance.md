# Metric provenance

Every reported metric must preserve its source label: owned event, Google Ads, Meta Ads, GBP, or downstream business system. Cross-source totals are only allowed when deduplication is explicitly defined.
